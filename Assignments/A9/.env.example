# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster> with your actual values
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/a9-books-db?retryWrites=true&w=majority

# Server port
PORT=3000